services:
<<<<<<< Updated upstream
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: swc
      POSTGRES_PASSWORD: swc
      POSTGRES_DB: swc
    ports:
      - "5432:5432"
    volumes:
      - swc_pg:/var/lib/postgresql/data
volumes:
  swc_pg:

=======
  postgres:
    image: postgres:16-alpine
    container_name: swc-postgres
    restart: unless-stopped
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-swc}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-swc}
      POSTGRES_DB: ${POSTGRES_DB:-swc}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-swc} -d ${POSTGRES_DB:-swc}"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
>>>>>>> Stashed changes
